{
	"$schema": "https://opencode.ai/config.json",
	"instructions": ["AI_NOTES/*.md", ".cursor/rules/*.md"],

	"mcp": {
		"docker": {
			"enabled": true,
			"type": "local",
			"command": ["docker", "mcp", "gateway", "run"]
		}
	}
}
